const RemoveAllcontact=(props) =>{
    return(
        <div>
    <button  className="btn btn-danger form-control" onClick={() =>props.handleRemoveAllcotact()}> Remove All</button>
        </div>
    );
}

export default RemoveAllcontact;